# Web API framework for inference service
fastapi==0.115.6

# ASGI server to run FastAPI
uvicorn[standard]==0.32.1

# Image handling + multipart upload support (for /predict later)
pillow==10.4.0
python-multipart==0.0.12

# Logging helpers (optional but useful)
structlog==24.4.0

# Testing framework
pytest==8.3.4
httpx==0.27.2
